# Pre-commit hooks
# Translation extraction/compilation handled by CI and build process

# Commit message validation
commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
