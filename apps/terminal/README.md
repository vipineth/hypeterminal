# apps/terminal

The HypeTerminal web app. React 19 + TanStack Start (SSR) + Vite 7.

Composes `@hypeterminal/hl-react` (data/signing) and `@hypeterminal/ui` (design system) into the product. See the [root README](../../README.md) for how it fits into the monorepo.

## Stack

| Layer | Choice | Version |
|---|---|---|
| Framework | TanStack Start (SSR meta-framework on Vite + Nitro) | 1.132 |
| Bundler | Vite | 7.3 |
| React | React + React Compiler (babel plugin) | 19.2 |
| Server state | TanStack Query | 5.90 |
| Client state | Zustand (+ `persist`) | 5.0 |
| Styling | Tailwind v4 + `@hypeterminal/ui` tokens | 4.0 |
| Wallets | wagmi + viem | 3.1 / 2.42 |
| Charting | klinecharts (+ optional TradingView via `types/charting_library`) | 9.8 |
| Bridging | `@lifi/sdk` | 3.16 |
| Numbers | `big.js` (via `lib/trade/numbers`) | 7.0 |
| Validation | Zod | 4.3 |
| i18n | Lingui (6 locales) | 5.7 |
| Tables / virtualization | `@tanstack/react-table` + `@tanstack/react-virtual` | — |
| Toasts / drawer | `sonner`, `vaul` | — |
| Tests | Vitest (+ WebSocket soak harness) | 4.0 |

Vite config is in `vite.config.ts`. Notable bits:

- `tanstackStart` + `nitroV2Plugin` for SSR
- `@vitejs/plugin-react` wired with `babel-plugin-react-compiler`
- `@lingui/vite-plugin` + `@lingui/babel-plugin-lingui-macro` for i18n
- `@tailwindcss/vite` for Tailwind v4
- Manual chunking splits vendors: `vendor-radix`, `vendor-tanstack`, `vendor-charts`, `vendor-web3`, `vendor-klinecharts`
- `rollup-plugin-visualizer` for bundle analysis

## Directory layout

```
apps/terminal/
├── src/
│   ├── routes/            file-based routes (TanStack Router)
│   ├── router.tsx         router + RootProvider wiring
│   ├── routeTree.gen.ts   auto-generated by the TanStack plugin
│   ├── providers/         RootProvider (Query, i18n, wagmi, hl-react, markets) · ExchangeScope
│   ├── stores/            Zustand stores (see below)
│   ├── hooks/             trade/, ui/, plus top-level (use-mobile, use-local-storage, ...)
│   ├── domain/            market/ and trade/ domain types + calculations
│   ├── lib/               trade/, chart/, search/, lifi/, hyperliquid/, tests/, format.ts, cn.ts, ...
│   ├── components/
│   │   ├── trade/         product UI (see below)
│   │   ├── ui/            app-specific compositions of @hypeterminal/ui primitives
│   │   ├── pages/         page shells
│   │   └── icons/         icon assets
│   ├── config/            constants, chains, ABIs, wagmi config, builder fees
│   ├── locales/           Lingui catalogs (en, zh, hi, es, fr, ar)
│   ├── bones/             `boneyard-js` auto-generated skeletons (pixel-perfect loaders)
│   ├── types/             charting_library types
│   └── styles.css         Tailwind entry + app-level tokens + font imports
├── vite.config.ts
├── lingui.config.ts
├── tsconfig.json          path alias: @/* → ./src/*
└── package.json
```

### `components/trade/` — the product UI

| Panel | Location |
|---|---|
| Chart | `chart/` — `kline-chart.tsx`, `tradingview-chart.tsx`, `token-selector.tsx`, `theme-colors.ts` |
| Tradebox (order entry) | `tradebox/` — `trade-panel.tsx`, `trade-form-fields.tsx`, `leverage-control.tsx`, `margin-mode-modal.tsx`, `tp-sl-section.tsx`, `deposit-modal.tsx`, `bridge-tab.tsx` |
| Orderbook & tape | `orderbook/` — `orderbook-panel.tsx`, `trades-panel.tsx` |
| Positions | `positions/` — `positions-tab.tsx`, `position-tpsl-modal.tsx`, `position-limit-close-modal.tsx`, `funding-tab.tsx`, `balances-tab.tsx` |
| Header | `header/` — `top-nav.tsx`, `user-menu.tsx`, `theme-toggle.tsx`, `favorites-strip.tsx` |
| Footer / layout | `footer/`, `layout/` |
| Mobile | `mobile/` — a parallel tree: `mobile-terminal.tsx`, `mobile-trade-view.tsx`, `mobile-orders-tab.tsx`, `mobile-chart-view.tsx`, etc. |

Mobile vs desktop is gated by the `md:` breakpoint and `use-mobile.ts`; the root layout renders two distinct skeletons during SSR.

### Routes

File-based, under `src/routes/`:

| File | URL |
|---|---|
| `__root.tsx` | root layout — wraps all routes in providers + skeletons |
| `index.tsx` | `/` |
| `perp.tsx` | `/perp` — perpetuals |
| `spot.tsx` | `/spot` — spot markets |
| `builders-perp.tsx` + `builders-perp.index.tsx` + `builders-perp.$dex.tsx` | `/builders-perp`, `/builders-perp/:dex` |
| `$.tsx` | splat fallback |

`routeTree.gen.ts` is auto-generated — don't edit it.

### Providers

`src/providers/root.tsx` composes:

- `QueryClientProvider` (TanStack Query)
- `I18nProvider` (Lingui)
- `WagmiProvider` + connectors (`config/wagmi.ts`)
- `HyperliquidProvider` from `hl-react` — configured with `env`, `builderConfig`, `agentName`
- `MarketsInfoProvider` — market metadata
- `ExchangeScopeProvider` — perp vs spot scope

### Zustand stores

All in `src/stores/`. `validated-storage.ts` wraps `localStorage` with Zod validation.

| Store | Purpose |
|---|---|
| `use-global-settings-store` | theme, decimals, leverage defaults, sidebar state |
| `use-market-store` | selected market per dex category |
| `use-order-entry-store` | order form inputs |
| `use-order-queue-store` | pending orders |
| `use-orderbook-actions-store` | orderbook UI interactions |
| `use-global-modal-store` | global modal visibility |

### `lib/` — utilities

| Subdir | Contents |
|---|---|
| `trade/` | order types, orderbook math, open-orders, TP/SL, margin mode, order validation, **`numbers.ts`** (big.js wrappers: `toBig`, `toSafeBig`, `toNumber`) |
| `chart/` | klinecharts adapters |
| `hyperliquid/` | markets context, `use-deposit`, hl-react re-exports |
| `search/` | market/asset search presets |
| `lifi/` | bridge SDK integration |
| `tests/` | vitest suites — unit + integration |

Top-level: `format.ts`, `cn.ts`, `i18n.ts`, `seo.ts`, `errors.ts`, `explorer.ts`, `network.ts`, `wallet-utils.ts`.

## Styling

`src/styles.css` is the entry. It imports:

```css
@import "tailwindcss";
@import "tw-animate-css";
@import "@fontsource-variable/inter";
@import "@hypeterminal/ui/globals.css";   /* design-system tokens */
/* app-level tokens + dark-mode variant follow */
```

App-level tokens extend the `@hypeterminal/ui` base. `@source "../../../packages/ui/src"` tells Tailwind to scan the UI package for class usage.

See `.claude/rules/design-tokens.md` for the token taxonomy (`bg-bg-sunken`, `text-text-strong`, `bg-market-up`, etc.) — **never use hex literals**.

## Testing

`src/lib/tests/` holds all vitest suites:

- Unit: `format.test.ts`, `errors.test.ts`, `orders.test.ts`, `trades.test.ts`, `seo.test.ts`, `ring-buffer.test.ts`
- Integration: `hyperliquid-hooks.test.ts`, `websocket-payload-guard.test.ts`
- Perf/soak: `websocket-soak.test.ts`, `websocket-store-benchmark.test.ts`

```bash
pnpm test                   # full vitest run
pnpm perf:ws-soak:30m       # 30-min WS reliability soak
pnpm perf:ws-soak:60m       # 60-min variant
pnpm perf:bundle            # bundle size analysis
```

Soak knobs: `WS_SOAK_RUN`, `WS_SOAK_DURATION_MS`, `WS_SOAK_SAMPLE_MS`, `WS_SOAK_OP_MS`.

## i18n

`lingui.config.ts` — source locale `en`, catalogs at `src/locales/{locale}/messages.po`. Locales: `en`, `zh`, `hi`, `es`, `fr`, `ar`.

`src/lib/i18n.ts` exposes `locales`, `localeList`, `numberFormatLocales`, and `dynamicActivate(locale)` (lazy-loads catalog via `import.meta.glob`). `en` is loaded synchronously on startup for SSR.

In components:

```tsx
import { t } from "@lingui/core/macro";
import { Trans } from "@lingui/react/macro";
```

## Environment

Real `.env*` files are gitignored. Copy `apps/terminal/.env.example` to `apps/terminal/.env.local` and fill in the values.

| Variable | Required | Default | Purpose |
|---|---|---|---|
| `VITE_WALLET_CONNECT_PROJECT_ID` | Required for WalletConnect | none | Enables WalletConnect connector and QR/deep-link flows |
| `VITE_ETHEREUM_RPC_URL` | No | `https://ethereum-rpc.publicnode.com` | Ethereum mainnet RPC used by bridge config |
| `VITE_HYPERLIQUID_TESTNET` | No | `false` | Defaults server-side network selection to testnet |
| `VITE_PERF_ENABLED` | No | `false` | Enables render profiling by default |

When `VITE_WALLET_CONNECT_PROJECT_ID` is absent or blank, the WalletConnect connector is omitted; `injected()` and Coinbase Wallet remain available.

## Conventions

See the root [README](../../README.md#conventions) and `.claude/rules/`. In this app specifically:

- Trade math goes in `lib/trade/`, not components.
- Components use `@hypeterminal/ui` primitives; app-specific compositions go in `components/ui/`.
- SSR: never touch `window`/`document` at module top-level. Use `ClientOnly` from `@tanstack/react-router` for client-only trees.
- No manual `useMemo`/`useCallback` for performance — the React Compiler handles it.
